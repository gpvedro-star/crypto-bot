import { StaticPage, staticPageMetadata } from "@/components/pages/StaticPage";

export const metadata = staticPageMetadata("editorial-standards");

export default function Page() {
  return <StaticPage slug="editorial-standards" />;
}

import { StaticPage, staticPageMetadata } from "@/components/pages/StaticPage";

export const metadata = staticPageMetadata("ai-usage-policy");

export default function Page() {
  return <StaticPage slug="ai-usage-policy" />;
}

node_modules
dist
.DS_Store
*.local

# Vendored photography — regenerate with `npm run photos`
public/photos/

# Single-file bundle output — regenerate with `npm run singlefile`
dist-single/

# Secrets — never committed
.env
.env.local

# Pexels working files
pexels.shortlist.json
